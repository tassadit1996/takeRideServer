type UpdateProfileResponse {
	ok: Boolean!
	error: String
}

type Mutation {
	UpdateProfile(
		firstname: String!
		lastname: String!
		email: String!
		password: String!
		profilePhoto: String!
		age: Int!
	): UpdateProfileResponse!
}
